import * as React from "react";

import * as Components from "@/components";

import * as Styled from "./create.styled";

import { Wrapper } from "../wrapper";

export const Create = () => {
	return (
		<Wrapper>
			<Styled.Create></Styled.Create>
		</Wrapper>
	);
};
